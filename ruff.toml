target-version = "py311"

[lint]
select = ["E", "F", "W", "B"]
ignore = [
    "E501",  # long lines are common in this codebase (dense simulation math); not worth reflowing
    "B905",  # zip() over equal-length sequences; strict= would add noise without catching real bugs
]
